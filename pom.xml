<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.casehub</groupId>
    <artifactId>casehub-parent</artifactId>
    <version>0.2-SNAPSHOT</version>
  </parent>

  <artifactId>casehub-connectors-parent</artifactId>
  <version>0.2-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>CaseHub Connectors — Parent</name>
  <description>Lightweight outbound message connectors for the CaseHub platform.
Provides a unified Connector SPI with built-in implementations for Slack (Incoming Webhooks),
Microsoft Teams (Adaptive Cards), Twilio SMS, WhatsApp Business API, and Email (quarkus-mailer).
Zero heavy dependencies — HTTP-based connectors use java.net.http only.</description>
  <url>https://github.com/casehubio/connectors</url>

  <modules>
    <module>core</module>
    <module>webhook</module>
    <module>email</module>
    <module>email-inbound</module>
    <module>mcp</module>
    <module>slack-bot</module>
    <module>chat-spi</module>
    <module>chat-ref</module>
    <module>chat-irc</module>
    <module>discord</module>
    <module>chat-discord</module>
    <module>signal-cli</module>
    <module>chat-signal</module>
    <module>chat-slack</module>
    <module>notification-bridge</module>
    <module>calendar-spi</module>
    <module>calendar-ref</module>
    <module>calendar-google</module>
    <module>graphql</module>
  </modules>

  <properties>
    <quarkus.version>${quarkus.platform.version}</quarkus.version>
    <quarkus-mcp-server.version>1.11.1</quarkus-mcp-server.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.wiremock</groupId>
        <artifactId>wiremock-standalone</artifactId>
        <version>3.9.1</version>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <repositories>
    <repository>
      <id>github</id>
      <url>https://maven.pkg.github.com/casehubio/*</url>
      <snapshots><enabled>true</enabled></snapshots>
    </repository>
  </repositories>



  <distributionManagement>
    <repository>
      <id>github</id>
      <url>https://maven.pkg.github.com/casehubio/connectors</url>
    </repository>
    <snapshotRepository>
      <id>github</id>
      <url>https://maven.pkg.github.com/casehubio/connectors</url>
    </snapshotRepository>
  </distributionManagement>

</project>
